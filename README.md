# Multi-label-toxic-comment-classifier
Multi-label toxic comment classifier using the Jigsaw dataset. Text is preprocessed, vectorized with TF‑IDF, and trained with Logistic Regression to predict six labels (toxic, severe_toxic, obscene, threat, insult, identity_hate). Evaluated via accuracy/precision/recall/F1 and confusion matrix; achieves 91.99% accuracy.
